# Set prefix key to Ctrl-a instead of Ctrl-b
unbind C-b
set -g prefix C-a
bind C-a send-prefix

# Start window numbering at 1
set -g base-index 1
setw -g pane-base-index 1

# Make window numbering consistent after closing windows
set -g renumber-windows on

# Enable vim mode
setw -g mode-keys vi
